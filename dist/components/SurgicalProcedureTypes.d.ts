import React from "react";
import { SurgicalProcedureType } from "../models/models";
export declare const SurgicalProcedureTypes: React.FC<{
    onItemClick: (item: SurgicalProcedureType) => void;
}>;
